import { Processor } from "./processor";
export declare class PropsProcessor extends Processor {
    private rawPropsBlocks;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveProps;
    restoreRawPropsBlock(content: any): Promise<any>;
    storeRawPropsBlock(value: any): any;
    getRawPropsPlaceholder(replace: any): any;
}
