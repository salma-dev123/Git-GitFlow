import { Processor } from "./processor";
export declare class BladeCommentProcessor extends Processor {
    private bladeComments;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveBladeComment;
    private restoreBladeComment;
    storeBladeComment(value: string): any;
    getBladeCommentPlaceholder(replace: any): any;
}
