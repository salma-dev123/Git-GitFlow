import { Processor } from "./processor";
export declare class XslotProcessor extends Processor {
    private xSlot;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveXslot;
    private restoreXslot;
    storeXslot(value: string): any;
    getXslotPlaceholder(replace: any): any;
}
