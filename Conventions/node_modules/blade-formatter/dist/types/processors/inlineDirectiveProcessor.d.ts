import { Processor } from "./processor";
export declare class InlineDirectiveProcessor extends Processor {
    private inlineDirectives;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveInlineDirective;
    private restoreInlineDirective;
    storeInlineDirective(value: any): any;
    getInlinePlaceholder(replace: any, length?: number): any;
}
