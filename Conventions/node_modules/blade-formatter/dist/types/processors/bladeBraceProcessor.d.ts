import { Processor } from "./processor";
export declare class BladeBraceProcessor extends Processor {
    private bladeBraces;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveBladeBrace;
    private restoreBladeBrace;
    storeBladeBrace(value: any, length: any): any;
    getBladeBracePlaceholder(replace: any, length?: number): any;
}
