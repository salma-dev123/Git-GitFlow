import { Processor } from "./processor";
export declare class XDataProcessor extends Processor {
    private xData;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveXData;
    private restoreXData;
    storeXData(value: string): any;
    getXDataPlaceholder(replace: any): any;
}
