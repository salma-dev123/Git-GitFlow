import { Processor } from "./processor";
export declare class ConditionsProcessor extends Processor {
    private conditions;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveConditions;
    private restoreConditions;
    storeConditions(value: any): any;
    getConditionsPlaceholder(replace: any): any;
}
