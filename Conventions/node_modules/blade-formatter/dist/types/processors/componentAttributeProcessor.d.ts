import { Processor } from "./processor";
export declare class ComponentAttributeProcessor extends Processor {
    private componentAttributes;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveComponentAttribute;
    private restoreComponentAttribute;
    storeComponentAttribute(value: string): any;
    getComponentAttributePlaceholder(replace: any): any;
}
