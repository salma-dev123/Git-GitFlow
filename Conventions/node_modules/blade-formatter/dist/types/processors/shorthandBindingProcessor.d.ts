import { Processor } from "./processor";
export declare class ShorthandBindingProcessor extends Processor {
    private shorthandBindings;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveShorthandBinding;
    private restoreShorthandBinding;
    storeShorthandBinding(value: string): any;
    getShorthandBindingPlaceholder(replace: string, length?: any): any;
}
