import { Processor } from "./processor";
export declare class HtmlAttributesProcessor extends Processor {
    private htmlAttributes;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveHtmlAttributes;
    private restoreHtmlAttributes;
    storeHtmlAttribute(value: string): any;
    getHtmlAttributePlaceholder(replace: string, length: any): any;
}
