import { Processor } from "./processor";
export declare class BladeDirectiveInStylesProcessor extends Processor {
    private bladeDirectivesInStyle;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveBladeDirectiveInStyles;
    private restoreBladeDirectiveInStyles;
    storeBladeDirectiveInStyle(value: string): any;
    getBladeDirectiveInStylePlaceholder(replace: string): any;
}
