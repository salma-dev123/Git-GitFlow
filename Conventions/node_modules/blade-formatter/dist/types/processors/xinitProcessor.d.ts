import { Processor } from "./processor";
export declare class XInitProcessor extends Processor {
    private xInit;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveXInit;
    private restoreXInit;
    storeXInit(value: string): any;
    getXInitPlaceholder(replace: any): any;
}
