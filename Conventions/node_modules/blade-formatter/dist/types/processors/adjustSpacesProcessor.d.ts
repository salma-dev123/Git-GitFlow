import { Processor } from "./processor";
export declare class AdjustSpacesProcessor extends Processor {
    private bladeComments;
    preProcess(_content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private adjustSpaces;
}
