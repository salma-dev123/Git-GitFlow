import { Processor } from "./processor";
export declare class IgnoredLinesProcessor extends Processor {
    private ignoredLines;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveIgnoredLines;
    private restoreIgnoredLines;
    private storeIgnoredLines;
    private getIgnoredLinePlaceholder;
}
