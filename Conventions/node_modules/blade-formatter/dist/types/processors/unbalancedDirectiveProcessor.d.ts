import { Processor } from "./processor";
export declare class UnbalancedDirectiveProcessor extends Processor {
    private unbalancedDirectives;
    preProcess(content: string): Promise<any>;
    postProcess(content: string): Promise<any>;
    private preserveUnbalancedDirective;
    private restoreUnbalancedDirective;
    storeUnbalancedDirective(value: string): any;
    getUnbalancedDirectivePlaceholder(replace: string): any;
}
