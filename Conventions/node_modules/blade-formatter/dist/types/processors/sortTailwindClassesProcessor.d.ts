import { Processor } from "./processor";
export declare class SortTailwindClassesProcessor extends Processor {
    preProcess(content: string): Promise<any>;
    postProcess(_content: string): Promise<any>;
    private sortTailwindClasses;
}
