import type Formatter from "./formatter";
export declare class FormatContentPipeline {
    private formatter;
    private processors;
    private preProcessors;
    private postProcessors;
    constructor(formatter: Formatter);
    formatContent(content: any): Promise<any>;
}
